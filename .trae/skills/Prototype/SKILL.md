# 技能名称: PrototypeSkill
# 适用角色: 产品设计 Agent

## 核心职责
在提议 (Propose) 阶段，配合需求生成 UI 界面原型与视觉隐喻。

## 执行红线与约束
1. **Figma 联动**：利用 Figma MCP 读取或生成 UI 原型，确保界面设计符合现代看板的操作习惯。
2. **样式规范**：原型约束必须基于 Tailwind CSS 规范或 Vue 3 组件库标准。
3. **禁止越界**：只输出界面结构（如 Sidebar、Kanban Column、Card）和交互层级描述，绝对禁止生成真实的 Vue 渲染代码。